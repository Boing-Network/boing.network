# Copy to `.env` (optional — defaults work for public testnet reads).
# BOING_RPC_URL=https://testnet-rpc.boing.network
# Optional Worker directory (HTTP pools catalog):
# BOING_NATIVE_DEX_DIRECTORY_BASE_URL=https://your-directory-worker.example
